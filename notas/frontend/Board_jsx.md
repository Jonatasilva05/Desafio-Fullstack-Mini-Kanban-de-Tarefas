### Board.jsx
- filter() - vamos imaginar que a lista possui 

<br>

    React → todo

    Go → doing

    Docker → done

    Node → todo

quando fazemos tarefas.filter(tarefa => tarefa.status === "todo") ele retorna apenas React -> node, ou seja cada coluna so ira mostrar a tarefa dos status

